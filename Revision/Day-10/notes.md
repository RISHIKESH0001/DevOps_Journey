# Day 10 Revision Notes

	Purpose				Command

	CPU/Processes			top

	CPU-heavy processes		ps aux --sort=-%cpu

	Memory				free -h

	Memory-heavy processes		ps aux --sort=-%mem

	Disk				df -h

	Directory size			df -sh

	Find large files		find

	Service status			systemctl status

	Service logs			journalctl -u

	Failed services			systemctl --failed

	Processes			ps, pgrep

	IP				ip addr

	Routes				ip route

	Listening ports			ss -tulpn

	HTTP test			curl

	Permissions			ls -l

	Current user			whoami

	Groups/IDs			id



